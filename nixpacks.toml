[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg", "python3", "gcc", "gnumake"]

[phases.install]
cmds = [
  "npm install",
  "pip install edge-tts --break-system-packages"
]

[start]
cmd = "node bot.js"

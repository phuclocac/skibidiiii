[phases.setup]
nixPkgs = ["nodejs", "ffmpeg", "gcc", "python3", "pkg-config"]

[phases.install]
cmds = ["npm install --ignore-scripts"]

[phases.build]
cmds = ["npm rebuild --build-from-source"]
